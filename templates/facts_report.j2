<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aruba Switches Facts Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f5f5f5;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        h1 {
            color: #0066cc;
            text-align: center;
            margin-bottom: 30px;
        }
        .summary {
            background-color: #f0f7ff;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #0066cc;
            color: white;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        tr:hover {
            background-color: #e6f2ff;
        }
        .timestamp {
            text-align: right;
            font-style: italic;
            color: #666;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Aruba Switches Facts Report</h1>
        
        <div class="summary">
            <h2>Summary</h2>
            <p>Report generated on: {{ timestamp }}</p>
            <p>Total switches: {{ groups['all'] | length }}</p>
        </div>
        
        <h2>Switch Details</h2>
        <table>
            <tr>
                <th>Hostname</th>
                <th>IP Address</th>
                <th>Model</th>
                <th>Software Version</th>
                <th>Serial Number</th>
                <th>Uptime</th>
            </tr>
            {% for host in groups['all'] %}
            {% if hostvars[host].ansible_facts is defined %}
            <tr>
                <td>{{ host }}</td>
                <td>{{ hostvars[host].ansible_host }}</td>
                <td>{{ hostvars[host].ansible_facts.ansible_net_model | default('N/A') }}</td>
                <td>{{ hostvars[host].ansible_facts.ansible_net_version | default('N/A') }}</td>
                <td>{{ hostvars[host].ansible_facts.ansible_net_serialnum | default('N/A') }}</td>
                <td>{{ hostvars[host].ansible_facts.ansible_net_system.uptime | default('N/A') }}</td>
            </tr>
            {% endif %}
            {% endfor %}
        </table>
        
        <h2>Group Distribution</h2>
        <table>
            <tr>
                <th>Group Name</th>
                <th>Number of Switches</th>
                <th>Switches</th>
            </tr>
            {% for group in groups | reject('equalto', 'all') | list | sort %}
            <tr>
                <td>{{ group }}</td>
                <td>{{ groups[group] | length }}</td>
                <td>{{ groups[group] | join(', ') }}</td>
            </tr>
            {% endfor %}
        </table>
        
        <div class="timestamp">
            Report generated by Ansible AWX on {{ timestamp }}
        </div>
    </div>
</body>
</html>